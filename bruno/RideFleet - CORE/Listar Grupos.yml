info:
  name: Listar Grupos
  type: http
  seq: 2

http:
  method: GET
  url: "{{CORE_HOST}}/groups/register"
  headers:
    - name: X-API-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
