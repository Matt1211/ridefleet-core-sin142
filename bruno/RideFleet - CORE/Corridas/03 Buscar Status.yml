info:
  name: Buscar Status
  type: http
  seq: 3

http:
  method: GET
  url: "{{CORE_HOST}}/rides/{{RIDE_UUID}}/status"
  headers:
    - name: X-API-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
