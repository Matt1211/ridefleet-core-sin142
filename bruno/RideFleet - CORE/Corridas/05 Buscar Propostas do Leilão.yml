info:
  name: Buscar Propostas do Leilão
  type: http
  seq: 5

http:
  method: GET
  url: "{{CORE_HOST}}/rides/{{RIDE_UUID}}/proposals"
  headers:
    - name: X-API-Key
      value: "{{API_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
